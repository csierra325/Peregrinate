import React from "react";

const TravelIdeasJumbotron = ({ children }) => (
  <div
    style={{ height: 1300, clear: "both", textAlign: "center" }}
    className="jumbotron"
  >
    {children}
  </div>
);

export default TravelIdeasJumbotron;
